{
  "name": "fastify-next-single-process",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx app/src/server/index.ts",
    "build": "rimraf dist && tsc -p tsconfig.json && next build ./web",
    "start": "cross-env NODE_ENV=production node dist/server/index.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/helmet": "^12.5.1",
    "@fastify/cookie": "^9.3.1",
    "next": "14.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "tsx": "^4.7.0",
    "@types/node": "^20.12.7",
    "eslint": "^9.0.0",
    "rimraf": "^5.0.5",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=18 <22"
  }
}